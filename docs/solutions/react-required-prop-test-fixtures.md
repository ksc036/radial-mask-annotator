# React Required Prop Test Fixtures

When adding a required prop to a React component, update every direct component test fixture in the same change. Run the focused component tests and the TypeScript build after the prop is threaded through the parent component.
